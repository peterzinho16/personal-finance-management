spring:
  profiles:
    active: dev
  jpa:
    hibernate:
      ddl-auto: update
    #https://stackoverflow.com/questions/30549489/what-is-this-spring-jpa-open-in-view-true-property-in-spring-boot
    open-in-view: false

azure:
  communication:
    email:
      connection-string: ${AZURE_COMM_EMAIL_CONNECTION_STRING}
      sender: donotreply@jaipro.pro